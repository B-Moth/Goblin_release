openai>=1.0.0
Pillow>=10.0.0
mutagen>=1.47.0
click>=8.0.0
flask>=3.0.0
markdown>=3.5.0
send2trash>=1.8.0
# Offline transcription (no API key required)
faster-whisper>=1.0.0
paddleocr>=2.8.0
paddlepaddle>=2.6.0
transformers>=4.42.0
torch>=2.2.0

# Optional: Ollama is required only for the local Qwen editing/runtime.
# Ollama is primarily a system binary; if a Python package/wrapper is available
# you can install it via pip. This line is optional and intended to help
# automated installers include local-model support when possible.
ollama>=0.1.0
